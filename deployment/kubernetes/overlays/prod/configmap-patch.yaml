# Replace `saintaugustin.example.com` with your real domain.
apiVersion: v1
kind: ConfigMap
metadata:
  name: saintaugustin-config
  namespace: saintaugustin
data:
  APP_URL: "http://saintaugustin.example.com"
  APP_FRONTEND_URL: "http://saintaugustin.example.com"
  MAIL_MAILER: "smtp"
